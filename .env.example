# Twilio Verify (SMS OTP for orders)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=

# HMAC secret for httpOnly cookie after successful SMS verify (min 32 random chars)
ORDER_PHONE_VERIFY_SECRET=

# Default region for parsing phone numbers (e.g. AT for Austria)
DEFAULT_PHONE_REGION=AT

# Order PDF + notification email (defaults to sakebestellen@gmail.com if unset)
ORDER_NOTIFY_EMAIL=

# SMTP (required for order emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
RESTAURANT_EMAIL=
